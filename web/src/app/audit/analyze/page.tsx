"use client";

export default function AnalyzePage() {
  // Phase 2: analyze page exists as a routing boundary only.
  // No UI is rendered here yet.
  // Logic is triggered from other steps (upload / review).

  return null;
}
